# Python 3.10+ baseline requirements for poke.AI
# GPU support is enabled on Linux/WSL2 via tensorflow[and-cuda].
tensorflow[and-cuda]==2.15.0.post1; platform_system=="Linux"
tensorflow==2.15.0; platform_system!="Linux"
# Apple Silicon GPU support (optional, macOS only)
tensorflow-metal; platform_system=="Darwin"

# Keep Keras 2.x to match this codebase and keras-resnet.
keras<3
keras-resnet

# Core scientific/vision stack
numpy
scipy
cython
Pillow
opencv-python
matplotlib

# Runtime utilities
mss
pyautogui
progressbar2
lxml
pandas
pyzmq

# Test tooling
pytest

# Optional (needed for COCO dataset tooling)
# pycocotools; platform_system!="Windows"
